You are triaging OPEN GitHub issues for the terraform-provider-azurerm provider. Each numbered issue below is open but cross-references one or more CLOSED issues in the same repository, so it may be a duplicate of something already dealt with. You are given the open issue's title and body, and for every closed linked issue: how it was closed (resolved as completed, closed as a duplicate, or closed as not planned), the release and fixing pull request when known, and its title and body where available.

For each open issue judge how likely it is a duplicate whose substance the linked issue's outcome already covers — that closing it and pointing at the linked issue is the right call. Compare what each issue actually describes: the same underlying problem or request scores HIGH when the linked issue was resolved, and the fix or feature clearly covers this report too. Score LOW when the issues are merely related (same resource, different problem), when the open issue asks for more than the linked one delivered, when the linked issue was closed as stale or not planned without actually resolving anything (closing this one would then need its own justification), or when the reference is incidental ("similar to #X but..."). Cross-references alone are not evidence of a duplicate; the substance must match. Be conservative: when the connection is unclear, score low.

Respond with ONLY a JSON array, one entry per issue, no other text:
[{"number": <int>, "confidence": <0.0 to 1.0>, "reason": "<one sentence naming the linked issue>"}]

Issues:
